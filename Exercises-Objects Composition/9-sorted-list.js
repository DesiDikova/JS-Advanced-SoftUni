function sortedList(params) {

    // add(element) - добавя нов елемент към колекцията
    // remove(index) - премахва елемента на позиция индекс
    // get(index) - връща стойността на елемента на позиция индекс
    // size - брой елементи, съхранявани в колекцията
}

let list = createSortedList();
list.add(5);
list.add(6);
list.add(7);
console.log(list.get(1));
list.remove(1);
console.log(list.get(1));
