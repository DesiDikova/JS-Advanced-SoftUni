function squareOfStars(num) {
    
    for (let row = 1; row <= num; row++) {
        
        console.log('* '.repeat(num));
    }
}

squareOfStars(1);
squareOfStars(2);
squareOfStars(5);
squareOfStars(7);